# Balrog API (FastAPI). Default matches local `uv run uvicorn ... --port 8000`.
VITE_API_BASE_URL=http://localhost:8000

# Run Analysis: meters to expand the clash bounding box when collecting nearby Speckle objects (default 2).
# VITE_CLASH_ANALYSIS_CONTEXT_EXPAND_METERS=2
